### model
model_name_or_path: /data/lhx/LLaMA-Factory/models/base/Qwen3-14B
trust_remote_code: true

### method
stage: sft
do_train: true
finetuning_type: lora
lora_target: all
deepspeed: examples/deepspeed/ds_z0_config.json

### dataset
dataset: merged_extract_label_cot
dataset_dir: /data/lhx/LLaMA-Factory/data
template: qwen
cutoff_len: 4096
max_samples: 9000
overwrite_cache: true
preprocessing_num_workers: 16

### output
output_dir: /data/lhx/LLaMA-Factory/saves/finetuned_reasoning/Qwen3-14B/Qwen3-14B-merged_extract_label_cot/lora/sft
logging_steps: 10
save_steps: 500
plot_loss: true
overwrite_output_dir: true

### train
per_device_train_batch_size: 1
gradient_accumulation_steps: 2
learning_rate: 5.0e-5
num_train_epochs: 3.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
ddp_timeout: 180000000

### eval
val_size: 0.1
per_device_eval_batch_size: 1
eval_strategy: steps
eval_steps: 500
